<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.h5190019.ibrahim_metehan_barutcu">

    <application
        android:name=".GameApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.EndGameActivity"
            android:theme="@style/EndGameTheme" />
        <activity
            android:name=".activities.WrongAnswerActivity"
            android:theme="@style/WrongAnswerTheme" />
        <activity android:name=".activities.GameMainPageActivity" />
        <activity android:name=".activities.CategoriesActivity" />
        <activity android:name=".activities.PlayerLoginActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>